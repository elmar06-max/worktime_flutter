# WorkTime Flutter

WorkTime is a Flutter app for tracking work sessions, splitting hours into daytime and overtime, and viewing monthly overviews.

## Development

Prerequisites:
- Flutter SDK

Getting started:
1. Install dependencies: `flutter pub get`
2. Run the app: `flutter run`
